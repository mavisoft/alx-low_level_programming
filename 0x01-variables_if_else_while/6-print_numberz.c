#include <stdio.h>

/**
 * main - Entry point
 * author: mavis
 * Return: Always 0 (Success)
 */
int main(void)
{
	int mav;

	for (mav = 0; mav < 10; mav++)
		putchar(mav);
	putchar('\n');
	return (0);
}
