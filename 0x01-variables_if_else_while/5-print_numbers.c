#include <stdio.h>

/**
 * main - Entry point
 * author: mavis
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	int mav;

	for (mav = 0; mav < 10; mav++)
	{
		printf("%d", mav);
	}
	printf("\n");
	return (0);
}
